# Public site (makemystay.ai) — Next.js + API proxy.
# Canonical copy for EC2: deploy/nginx-makemystay-next.conf
# Setup: deploy/README-next-ec2.md  |  Install: deploy/install-next-on-ec2.sh
#
# After edits on server:
#   sudo cp deploy/nginx-makemystay-next.conf /etc/nginx/sites-available/makemystay
#   sudo nginx -t && sudo systemctl reload nginx
#   sudo systemctl restart makemystay-next

# See deploy/nginx-makemystay-next.conf for the full active configuration.

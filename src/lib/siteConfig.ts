/** Canonical public site URL (no trailing slash). */
export const SITE_URL = 'https://makemystay.ai';

/** Marketing / SEO title suffix (react-helmet, Open Graph). Use consistently so GSC titles are not mixed. */
export const SITE_NAME = 'MakeMyStay.ai';

/** Legal entity name for policies and contracts (may differ from SITE_NAME). */
export const LEGAL_ENTITY_NAME = 'MakeMyStay Realty';

/** Must be an absolute URL that returns 200 in production (no /public/og-image.jpg in repo). */
export const DEFAULT_OG_IMAGE =
    'https://images.unsplash.com/photo-1600596542815-ffad4c1539a9?auto=format&fit=crop&w=1200&h=630&q=80';

/** Open Graph locale (India / English). */
export const OG_LOCALE = 'en_IN';

/** Twitter / X handle for twitter:site (matches public X profile). */
export const TWITTER_SITE_HANDLE = '@Makemystay16268';
